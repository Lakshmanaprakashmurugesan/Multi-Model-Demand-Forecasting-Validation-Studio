# Generated Results

Store only real outputs generated from an identified repository version and documented run.

For each retained run, preserve a corresponding completed run manifest in `evidence/` containing the commit SHA, dataset hash, configuration, execution date, and environment.

Do not commit fabricated forecast metrics or empty files that could be mistaken for actual measured outputs.
